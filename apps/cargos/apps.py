from django.apps import AppConfig


class CargosConfig(AppConfig):
    """Configura o app de cargos."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.cargos"
    label = "cargos"
