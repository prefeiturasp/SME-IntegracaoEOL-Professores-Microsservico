## Docker de Professores no dev:

services:
  professores:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: sme_sgp_ms_professores_dev
    env_file:
      - .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings
    volumes:
      - .:/app
    command: >
      python -Xfrozen_modules=off -m debugpy
      --listen 0.0.0.0:5678 manage.py runserver 0.0.0.0:8001
    ports:
      - "${PORT_WEB:-8004}:8001"
      - "${PORT_DEBUGPY:-5680}:5678"
    networks:
      - default
      - ms_etl_net
    restart: unless-stopped

networks:
  ms_etl_net:
    name: sme-integracaoeol-ms-etl_default
    external: true